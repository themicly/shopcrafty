# Reports module

Reports provides read-only admin dashboards and report screens over core
catalog, customer, order, and payment data. It does not own those domain
records.

AI summaries are optional integrations. The reports UI checks the ai
capability before resolving the AI service; the AI package owns provider
credentials and completion behavior.
